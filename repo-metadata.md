# GitHub repository metadata

## Suggested description
An event-driven serverless order workflow demonstrating asynchronous processing, least-privilege infrastructure, Infrastructure as Code, structured logging and scalable cloud architecture.

## Suggested topics
`python`, `aws`, `lambda`, `api-gateway`, `dynamodb`, `sqs`, `serverless`, `sam`, `infrastructure-as-code`, `event-driven`, `cloud`

## Suggested social preview
Use the project title + subtitle + 3–5 primary technologies. Keep the design consistent across all five repositories.

## Pinning priority
3/5
