# Database migrations

This project does not require a relational schema in the local baseline. If a managed database is introduced in production, keep schema changes versioned and reviewed as migration files in source control.
